<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_dashboard"
    tools:context=".ui.notifications.NotificationsFragment">

    <!-- Bande supérieure moderne avec glassmorphism -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp"
        android:background="@drawable/bg_header_gradient"
        android:elevation="4dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/text_info"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Appuyez longuement sur la carte pour ajouter une position"
            android:textSize="16sp"
            android:textColor="@android:color/white"
            android:fontFamily="sans-serif-medium"
            android:textAlignment="center"
            android:alpha="0.95"
            android:letterSpacing="0.02" />
    </LinearLayout>

    <!-- Carte avec coins arrondis et ombre moderne -->
    <androidx.cardview.widget.CardView
        android:id="@+id/map_card"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="24dp"
        app:cardCornerRadius="30dp"
        app:cardElevation="12dp"
        app:layout_constraintTop_toBottomOf="@id/header"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <fragment
            android:id="@+id/map"
            android:name="com.google.android.gms.maps.SupportMapFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </androidx.cardview.widget.CardView>

    <!-- Bouton flottant avec effet glassmorphism -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btn_add_position"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="Ajouter une position"
        android:src="@drawable/id_add_location"
        app:tint="@android:color/white"
        app:backgroundTint="#1A237E"
        app:borderWidth="0dp"
        app:layout_constraintBottom_toBottomOf="@id/map_card"
        app:layout_constraintEnd_toEndOf="@id/map_card"
        android:layout_margin="24dp"
        app:elevation="12dp"
        app:fabSize="normal"
        app:rippleColor="#80FFFFFF" />

</androidx.constraintlayout.widget.ConstraintLayout>